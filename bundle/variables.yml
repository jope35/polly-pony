variables:
  root_catalog:
    description: "The catalog to use for the root schema."
    default: jdetheije
  root_schema:
    description: "The schema to use for the root schema."
    default: default
