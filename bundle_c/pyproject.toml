[project]
name = "bundle-c"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["holiday>=1.0.0", "polly-pony-utils"]

[tool.uv.sources]
polly-pony-utils = { path = "..", editable = true }
